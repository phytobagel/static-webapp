// Project Settings → API in Supabase. The anon key is safe in the browser with RLS.
// For a public GitHub repo, prefer injecting this file in CI from GitHub Secrets instead.

export const supabaseUrl = "";
export const supabaseAnonKey = "";
